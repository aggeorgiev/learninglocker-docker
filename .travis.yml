sudo: required

services:
- docker

install: true

script:
- docker build -t ileda/learninglocker-app:"$TRAVIS_COMMIT" app
- docker build -t ileda/learninglocker-nginx:"$TRAVIS_COMMIT" nginx
- if [ "$TRAVIS_PULL_REQUEST" == "false" ] && ( [ "$TRAVIS_BRANCH" == "master" ] || [ "$TRAVIS_BRANCH" == "develop" ] ); then
    docker login -u="$DOCKERHUB_USERNAME" -p="$DOCKERHUB_PASSWORD";
    docker push ileda/learninglocker-app:"$TRAVIS_COMMIT";
    docker push ileda/learninglocker-nginx:"$TRAVIS_COMMIT";
    fi

